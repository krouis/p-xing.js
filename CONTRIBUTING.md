# Contributing

## Development setup

```bash
npm install
npm run dev
```

## CI checks

```bash
npm run ci
```

## Terminology

Always use **pixel crossing**. See `CLAUDE.md` for the terminology rule and the forbidden-term CI check.

## Code style

- TypeScript strict mode
- Prettier for formatting (`npm run format`)
- ESLint for linting (`npm run lint`)
